import { FC } from 'react';

import styles from './BackgroundOpacity.module.scss';

const BackgroundOpacity: FC = () => {
	return <div className={styles.background__opacity}></div>;
};

export default BackgroundOpacity;
